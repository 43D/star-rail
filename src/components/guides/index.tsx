type props = {
    _observer: number;
}

export const GuidesIndex = ({ _observer }: props) => {
    _observer;

    return (
        <div className="container-fluid" style={{ minHeight: "75vh" }}>
            guides
        </div>
    );
}